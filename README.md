# platformer-game
# Jogo Platformer Básico

Um jogo platformer simples desenvolvido em Python usando a biblioteca `pgzero`.

## Como Executar

1. Instale o `pgzero`:
   ```bash
   pip install pgzero
